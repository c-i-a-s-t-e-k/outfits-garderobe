---
change_id: garment-lifecycle
title: Edycja i usuwanie ubrania z oznaczeniem niekompletnych outfitów
status: implementing
created: 2026-09-14
updated: 2026-09-14
archived_at: null
---

## Notes

z @context/foundation/roadmap.md
- 2026-09-14 plan review (`reviews/plan-review.md`): 7 findings, all FIXED in `plan.md` — blank name in *Edit outfit* keeps the name (F2), edit page tells that adding garments does not close missing slots (F3), Phase 0 commits pending docs first and check 0.3 allows docs commits (F4), Phase 5 close-out also updates S-06 prerequisites/PRD refs, adds a *Parked* entry for orphan files and stamps `outfit-lifecycle/change.md` (F5). Verdict after fixes: SOUND.
- 2026-09-14 Merged master after S-04: merge commit `6d34fcd` (`chore(garment-lifecycle): merge master after S-04 (p0)`), on top of the docs commit `40051a5`; S-04 = PR #7 (`388e713`). No conflicts, including `roadmap.md`. Every S-04 contract in Phase 0 change 4 holds in the merged code: `discard_private_image` (row now, file `on_commit`); `NormalizedPhotoMixin.photo` with `clean_photo()` calling `validate_max_size` unconditionally; `stored_private_image(owner, file, original_filename='')` is a `@contextmanager`; `Outfit.photo` is `OneToOneField(PrivateImage, RESTRICT, null=True, blank=True, related_name='outfit')` with `photo_url`, and `0003_outfit_photo` is the latest `outfits` migration; `make_outfit(..., photo=False)`; `_seed_wardrobe` seeds `outfits[0]` tagged with a photo and `outfits[1]` bare; `photo-shrink.js` wires `form[data-submit-once]`; compose redirects to the outfit; `.outfit-photo img` exists; the outfit page has a *Photo* section and `_render_detail(request, outfit, form, photo_form=None)`; wardrobe tiles are one `<a>` holding `.outfit-preview outfit-preview-photo` or `.outfit-preview outfit-preview-N`. One naming drift, no semantic change: the pinned query-count tests are `garments/tests/test_views.py:204 test_list_query_count_does_not_grow_with_garments`, `outfits/tests/test_views.py:106 test_picker_query_count_does_not_grow_with_garments`, `:383 test_grid_query_count_does_not_grow_with_outfits_or_garments`, `:1188 test_grid_query_count_does_not_grow_with_outfits_tags_selections_or_photos` (the plan names it `..._selections`, and cites `:374,817`), `:632 test_detail_query_count_does_not_grow_with_tags` (plan `:623`) plus S-04's `:933 test_detail_query_count_is_the_same_with_and_without_a_photo`. Local dev commands need `DEBUG=True uv run --env-file …` (lessons). Checks: migrate OK, `makemigrations --check` no changes, `pytest` 294 passed, `ruff check` clean.
- 2026-09-14 Phase 0 manual 0.8 confirmed by the developer. Agreed plan adjustment: the query-count references in the Phase 2, 3 and 4 test contracts now name the real tests and their post-merge lines (`:204`, `:632`/`:933`, `:1188`). No other contract changed.
- 2026-09-14 Phase 1 adaptation: the foreign-write scenario checks `row.owner_id` on rows a successful write stored, and `MissingGarment` has no owner field, so the scenario reads it through a small `_owner_id(row)` helper (`outfit.owner_id` for a missing garment). The migration was ruff-formatted like the earlier ones. A mutation check (a no-op `post_delete` receiver on `MissingGarment`) makes the account-deletion test fail, as the Key Discovery says it should. Manual 1.8 run in headless Chromium against the dev server on :8004: admin delete of a garment used by two outfits → both outfit change pages show a read-only *Missing garments* row "Shoes — brown loafers", DB re-read confirms each outfit kept its other garment.
