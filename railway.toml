[build]
builder = "nixpacks"

[deploy]
startCommand = ".venv/bin/python manage.py migrate && .venv/bin/gunicorn --bind 0.0.0.0:$PORT outfits_garderobe.wsgi:application"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
